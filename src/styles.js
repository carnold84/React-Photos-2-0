import styled from 'styled-components';

export const Content = styled.div`
  flex-grow: 1;
  position: relative;
`;

export const Title = styled.h1`
  font-size: 1.8rem;
  line-height: 1rem;
  font-weight: normal;
  margin: 0 0 25px;
  align-items: center;
  display: flex;
`;
